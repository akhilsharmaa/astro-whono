
---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time 
  class="text-xl text-gray-500 whitespace-nowrap" 
  datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: '2-digit', 
		})
	}
</time>
